apiVersion: v1
kind: Service
metadata:
  name: {{ include "karna.fullname" . }}-agent
  labels:
    {{- include "karna.componentLabels" (dict "context" . "component" "agent") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "karna.selectorLabels" (dict "context" . "component" "agent") | nindent 4 }}
