apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "karna.fullname" . }}-skills
  labels:
    {{- include "karna.labels" . | nindent 4 }}
data:
  {{- range $path, $_ := .Files.Glob "files/skills/*.md" }}
  {{ base $path }}: |
    {{- $.Files.Get $path | nindent 4 }}
  {{- end }}
