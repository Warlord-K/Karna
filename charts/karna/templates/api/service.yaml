apiVersion: v1
kind: Service
metadata:
  name: {{ include "karna.fullname" . }}-api
  labels:
    {{- include "karna.componentLabels" (dict "context" . "component" "api") | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 8081
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "karna.selectorLabels" (dict "context" . "component" "api") | nindent 4 }}
